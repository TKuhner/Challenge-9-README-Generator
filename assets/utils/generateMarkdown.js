function generateMarkdown({ title, description, installation, usage, license, contributing, tests, questions }) {
    return console.log("made it to generateMarkdown.js" +  title + description + installation + usage + license + contributing + tests + questions);
}

module.exports = generateMarkdown;